# API base URL (no trailing slash). Defaults to http://localhost:8080 if unset.
VITE_API_URL=http://localhost:8080

# Storefront origin for Google Merchant CSV product links (no trailing slash).
# VITE_STOREFRONT_URL=https://example.com
